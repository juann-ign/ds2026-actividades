services:
  api:
    build: ./backend
    ports:
      - "3000:3000"
    env_file:
      - backend/.env
    volumes:
      - ./backend/src:/app/src
    depends_on:
      - db

  db:
    image: postgres:16-alpine
    ports:
      - "5433:5432"
    env_file:
      - backend/.env
    volumes:
      - pgData:/var/lib/postgresql/data

volumes:
  pgData:
